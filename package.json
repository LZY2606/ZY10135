{
  "name": "lineage-editor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local editor for cell-contour lineage tracking (links, divisions, merges, occlusions, reviewed hypotheses)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
